{
  "name": "beamcss-website",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "predev": "node scripts/sync-starter.mjs",
    "build": "astro build",
    "prebuild": "node scripts/sync-starter.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "node scripts/sync-starter.mjs",
    "og": "node scripts/build-og.mjs",
    "icons": "node scripts/build-icons.mjs",
    "contrast": "node scripts/check-contrast.mjs",
    "links": "node scripts/check-links.mjs",
    "test": "node --test packages/postcss-beam-fluid/test/fluid.test.js",
    "verify": "pnpm test && pnpm contrast && pnpm build && node scripts/verify-build.mjs && node scripts/check-links.mjs"
  },
  "dependencies": {
    "@fontsource-variable/instrument-sans": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@fontsource/instrument-serif": "^5.3.0",
    "astro": "^7.2.2",
    "postcss-beam-fluid": "workspace:*"
  },
  "devDependencies": {
    "postcss": "^8.5.26",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1",
    "sharp": "^0.35.3"
  }
}
